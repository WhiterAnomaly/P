// Inicializa o sistema de renderização
Core4.render.init("gameCanvas");

// Inicializa o sistema de entrada
Core4.input.init();

// Define o estado inicial da engine
Core4.flow.change("boot");

// Inicia o Game Loop
Core4.loop.start();

// Inicializa os sistemas
Core4.render.init("gameCanvas");
Core4.input.init();

// Estado inicial da engine
Core4.flow.change("boot");

// Inicia o Game Loop
Core4.loop.start();
